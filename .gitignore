# Build
target/
build/
bin/
out/
*.class
*.jar
*.war

# IDE
.idea/
*.iml
.vscode/
.settings/
.project
.classpath
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Gradle
.gradle/
gradle-app.setting

# Maven
dependency-reduced-pom.xml

# Logs
*.log

# Reference docs — authored .md files are tracked; vendor binaries are not
docs/reference/vendor/

# Vendor binaries (product zips, license files, extracted SDKs)
binaries/

# Docker
docker-compose.override.yml
.sdk-decompile/

# Local vendor libraries — track structure/docs, ignore JARs
libs/**/*.jar
# Exception: PA SDK is a 128 KB compile-only JAR (compileOnly + shadow-excluded).
# Committed so CI can build adapter-pingaccess without manual provisioning.
!libs/pingaccess-sdk/pingaccess-sdk-9.0.1.0.jar

# Audit reports — ephemeral session artifacts, not tracked
audit-reports/

# E2E Docker lifecycle marker
.e2e-infra-started
